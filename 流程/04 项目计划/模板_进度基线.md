# 进度基线

`{project_id}`　`{start_date}`　`{vertical}` / `{city}`　`sop_stage`＝（抄 02）　`{freeze_id}`＝（抄 03）

只保留当前 `sop_stage` 对应的行，删掉其余行：

| 窗 | 工作 | `plan_hours` | 完成定义 |
|---|---|---|---|
| 第 0 天 | 冻结配置、环境、试问 API |  | `{freeze_id}` 存在且 checksum 写入 |
| 噪声窗 | 无干预；P0 每天 1 轮或两个半天 |  | 至少两天正式行 |
| 基线窗 | 与噪声同词表；Core 抽检 100% |  | 定向级一页纸，四选一勾描述 |
| 干预窗 | 仅冲刺；一轮一类证据 |  | 干预台账有完成日 |
| 等待 | 3–7 天 |  | 不采正式复测 |
| 复测窗 | 仅冲刺；`window_class=execution_05` |  | 默认受控前后描述；不是 03 基线 |
| 监测窗 | 续约 weekly + calib |  | 滚动表，不重开因果 |

`Σ plan_hours` 必须 >0 且 ≤ 07 `budget_hours`。空包非法。  
里程碑签字：G2________　G3________　G4________（门名只写 G2，不用 G2′）
