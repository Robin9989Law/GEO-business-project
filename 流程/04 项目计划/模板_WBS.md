# WBS

`{project_id}`　锁定的 `sop_stage`　`{freeze_id}`（来自 03）

按核心链分解，不要把 04 排到 03 前面：

1. 01–02 已完成（本计划不重做）  
2. 03 测量（已冻结则只列剩余采集）  
   2.1 诊断：`noise` / `baseline`  
   2.2 冲刺才有：`retest`  
   2.3 续约才有：`weekly` / `calib`  
3. 04 本计划文件本身  
4. 05 实施  
   4.1 **仅冲刺**：一类证据（范围 = `treat_need_ids`）  
   4.2 周报（`verdict_4` 抄 03）  
5. 06 交付（路径 `流程/03 测量/出数/`）  

`sop_stage=诊断` 时删除 2.2、4.1。  
`sop_stage=续约` 时删除 4.1，保留 2.3。  
不得把「写作 / 投放 / 改页」写进诊断 WBS。
