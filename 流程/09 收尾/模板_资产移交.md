# 资产移交

对照 `流程/03 测量/配置/asset_policy.csv`。

- `close_assets_ok`：是 / 否（必须是才过 G8）
- `close_archive_ok`：是 / 否（引擎实查：无签出锁 + deposits 有本案 + 移交件在 + 脱敏通过；不是只看无锁）
- `asset_deposit.py` 已跑：是 / 否
- `资产库/登记/deposits.csv` 有本案 `project_anon`：是 / 否
- 冲刺是否在 `plays.csv` 补了一行（只类型，无客户 URL）：是 / 否 / 非冲刺
- 禁止进库项抽查（facts、截图、电话、点名问、fingerprint、客户全称）：通过 / 失败
- 旧城数字未写成新单基线：确认

失败则 G8 不通过。
