# 需求规格

上游：02 章程。本步写入 `treat_need_ids` `holdout_need_ids` `platforms_required`。  
03 的 `queries.csv` / `platforms.csv` 必须与这两列一致；05 干预不得超出 `treat_need_ids`，不得改 `holdout_need_ids`。

- `{project_id}`：
- 信息需求（处理组 `need_id`，每条至少 2 种 style 的释义）：  
  - N__ ：（发现 / 对比 / 入口 / 其他，用业务语言写，不要抄演示词表）
- 监测组 `need_id`（同品类、同城、本轮不优化）：  
  - H__ ：
- 平台：P0 必测列表（从 `流程/03 测量/配置/platforms.csv` 勾选，不得少写一家还称覆盖主流）
- 产品模式：当天只允许 `standard` 或 `deep_search` 一种
- 竞品集合：书面披露规则（合格竞品 = 别名表 competitor 且切片出现 ≥2 次）
- 点名问法：另表，不进主验收
- 假设：干净号冷会话；定位切到 `{city}`
