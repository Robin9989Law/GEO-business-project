# 02 需求 · 节点提示词

先读 `AGENTS.md` 与 `合同/核心合同.md` 第 1 节。

入口：G0。唯一任务：锁定可灌 03 的字段。人交章程材料；Agent 查 need 互斥、来源、验收三套。处理组∩监测组必须空。产品线只许收窄。正式产物：章程/规格/验收。G1 硬检。打回：补来源或拆开相交 need。归宿 `原始/02/`。下一站 07。

## 你在哪

把 01 的意向锁成验收合同。主终点、产品线、处理组/监测组、平台在这里一次性锁死。

上游必须已有：G0、品类、城市、代号、意向 `sop_stage`。  
下游：07 按本步范围估人时；08 按本步允许集锁对外口径；03 只灌入配置，不许另立口径。

门：**G1**。

## 先弄清的情况

- 01 意向是什么？本步只能相等或收窄（诊断不能升冲刺）。  
- 处理组 `treat_need_ids`：本轮要动的信息需求。  
- 监测组 `holdout_need_ids`：同品类同城、本轮不优化；与处理组不能相交。  
- 平台默认 P0，分平台出数，禁止平均。  
- `primary_endpoint` 只能 `p_mention`；`causal_claim` 默认 `descriptive_until_isolation`。  
- 三套验收规则必须写全且与产品线一致：`success_rule_diagnosis`（描述基线/不能下结论）、`success_rule_sprint`（受控前后描述/确认性 L1/不能下结论）、`success_rule_retain`（只能不能下结论）。

## 你要做

1. 告诉人：现在锁需求；先定产品线和 need，再谈预算和沟通，还不到采集。  
2. 读本目录三份模板 + `流程/03 测量/项目接口.md` 的灌入表。  
3. 起草章程 / 需求规格 / 验收标准到 `out/`，字段与 `合同/核心合同字段.csv` 一致。  
4. 停，等人批 G1。

## 需要人做

1. 确认或修改 need 列表与平台名单。  
2. 确认验收三套规则不另发明 KPI。  
3. 批准锁定。

## 材料放哪

| 材料 | 路径 |
|---|---|
| 信息需求清单 | `agent_pm/cases/{本案}/inbox/02_need清单.md` |
| 竞品如何算合格 | `agent_pm/cases/{本案}/inbox/02_竞品规则.md` |
| 章程/规格/验收草稿 | `agent_pm/cases/{本案}/out/02_*.md` |

G1 通过后先打开 07，不要直接去采。锁定字段在 03 才灌进 `流程/03 测量/配置/project.csv`。

## 然后

打开 `流程/07 预算和资源管理/AGENT.md`。
