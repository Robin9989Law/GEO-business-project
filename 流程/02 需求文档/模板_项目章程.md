# 项目章程

上游：01 商机卡。本步锁定 `sop_stage`（≤ 01 意向）、`primary_goal`。抄入案件合同卡「02」栏。

- `{project_id}`：
- `{client_code}`：
- `{owner}`：
- `{vertical}` / `{city}`：
- `{sop_stage}`：诊断 / 冲刺 / 续约
- `{start_date}`：
- `primary_goal`（只允许可见性原话）：在无品牌发现问上提高被正确提及的概率。
- 估计量：`platform_need_equal_ATT`；总体 = 冻结词表均值，不是全市用户。
- 不在章程内：保证推荐、保证报名、全市用户可见性。
- 主要风险：对照溢出、人手砍轮次、词表漏检、App 改版。
- 批准：________　　日期：________
