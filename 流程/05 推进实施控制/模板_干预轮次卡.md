# 干预轮次卡

`sop_stage` 不是冲刺则整卡写「不适用」，不要填类型。  
一轮只允许一类证据。`need_id` 必须 ∈ 02 锁定的 `treat_need_ids`。`holdout_need_ids` 本轮不改页。

- `{project_id}`　`sop_stage`：　轮次号：
- `intervention_class`（只勾一个）：□ 自有页　□ FAQ　□ 列表收录　□ 视频　□ 其他（写一种）　□ 不适用
- 对应处理组 `treat_need_ids` 子集：
- 监测组本轮是否改页：必须否
- 计划完成日 / 实际完成日 / 等待天数：
- 写入 `流程/03 测量/配置/intervention_ledger.csv`：是 / 否
- 复测任务：`retest`；话术默认受控前后描述
