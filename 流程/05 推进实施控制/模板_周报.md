# 周报

`verdict_4` 必须与 03 报告相同，不得自行升级。`sop_stage=诊断` 时本周任务不得含一类证据。

- `{project_id}`　`{vertical}` `{city}`　周次：待填　`sop_stage`（抄 02）
- `{freeze_id}`（抄 03）
- 本周任务：noise / baseline / weekly / calib / retest / 一类证据（只勾该 `sop_stage` 允许的）
- 覆盖：P0 是否齐全
- 数据等级：定向级 / 决策级（精度，不是效果）
- 结论（四选一，只勾一个）：  
  - [ ] 仅描述基线 / 噪声底  
  - [ ] 受控前后描述  
  - [ ] 确认性 L1（须隔离 + DiD CI 不含 0）  
  - [ ] 不能下结论
- 风险与下周：待填
- 禁止出现：保证推荐、报名增长、API 当作用户所见、CI 错开当效果
- 冲刺复测门禁（确认性 L1 必须全是）：`did_excludes_zero` `did_positive` `treat_clusters`（≥2） `holdout_clusters`（≥2） `coverage_ok`
