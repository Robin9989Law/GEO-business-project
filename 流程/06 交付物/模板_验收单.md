# 验收单

上游：02 验收标准 + 03 报告 + 05 周报。`verdict_4` 三处必须相同。`sop_stage=诊断` 只许收「描述基线」或「不能下结论」。

- `{project_id}`　`{vertical}` `{city}`　`sop_stage`（抄 02）　日期：
- `{freeze_id}`（抄 03）　`data_grade`（抄 03）
- `primary_endpoint`：p_mention
- `causal_claim`：descriptive_until_isolation（除非 02 已锁隔离）
- 对照文件：`流程/02 需求文档` 验收标准 + `流程/03 测量/案件/{本案}/出数/`
- `delivery_manifest_checksum`：（覆盖 metrics_daily + coverage + evidence_manifest + 客户报告）
- `freeze_match`：是 / 否（出数身份列与本案冻结一致，不是只对冻结目录）
- `delivery_accepted`：是 / 否（未接收不得进 09）
- 覆盖齐全（`platforms_required`）：是 / 否
- 抽检：待审 / 已抽检
- 四选一 `verdict_4` 与 03、05 一致：是 / 否
- 是否出现禁售句：是 / 否（是则拒收）
- 结论：接收 / 拒收 / 补件
- 签字：客户________　　负责人________
