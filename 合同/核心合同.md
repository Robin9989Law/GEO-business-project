# 核心合同（01→09 全流程唯一总线）

全流程只有这一条，顺序不得颠倒。文件夹编号是标签，行走顺序如下：

```text
01 洽谈 → 02 需求 → 07 预算 → 08 沟通 → 03 测量 → 04 计划 → 05 实施控制 → 06 交付 → 09 收尾
```

07 预算和 08 沟通在测量前锁定，不是交付后的附件。09 收尾在 G4 之后关门，G4 通过不是结案。

Agent 经 `agent_pm/run.py` 写 `state.json`。[案件合同卡.md](案件合同卡.md) 是字段说明。规格层目录见 [README.md](README.md)。后一阶段不得改已锁定字段；人 `CHANGE` 后 Agent 从锁定点重做。

## 1. 战略逻辑（口径只锁一次）

| 字段 | 锁在 | 允许值 | 01 | 02 | 07 | 08 | 03 | 04 | 05 | 06 | 09 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `sop_stage` | 02（01 只提意向） | 诊断 / 冲刺 / 续约 | 意向 | **锁** | 报价范围抄此 | 对外话术抄此 | 只读 | 只读 | 只读 | 只读 | 结项不得改口 |
| `primary_goal` | 02 | 无品牌发现问上提高被正确提及的概率 | 改口 | **锁** | 只读 | 只读 | 抄入 project.csv | 只读 | 只读 | 只读 | 对照章程关闭 |
| `primary_endpoint` | 02 | `p_mention` | 主终点=提及 | **锁** | 只读 | 只读 | 出数主列 | 只读 | 周报不得换 KPI | 验收只认此列 | 只读 |
| `causal_claim` | 02 | 默认 `descriptive_until_isolation` | 禁售确认性 | **锁** | 报价排除 L1 | 沟通不得升级 | 出数 verdict | 计划不得假设 L1 | 干预不得升级口径 | 验收对照此值 | 不得重开 L1 |
| `control_design` | 02 | 默认监测组，非反事实 | 写进范围 | **锁** | 监测组采集人天 | 报告分 core/holdout | 对照设计 | 进度含监测组采集 | 监测 need 不改页 | 报告分 core/holdout | 只读 |
| `success_rule_*` | 02 | 三套规则，见验收标准 | 产品线对应一套 | **锁** | 报价对应一套 | 对外只说该套 | 报告四选一必须落在该套内 | 进度完成定义抄此 | 周报四选一抄此 | 验收四选一抄此 | 结项抄此 |

战略推导（必须逐条成立）：

| `sop_stage` | 07 只许报 | 03 只跑 | 04 只许排 | 05 只许做 | 06 只许收 | 09 只许关 |
|---|---|---|---|---|---|---|
| 诊断 | 冻结+噪声+基线+抽检 | `noise` + `baseline` | 第 0 天、噪声窗、基线窗 | 禁止改页 / 发文 | 四选一只勾「描述基线」或「不能下结论」 | 不得写成有效果 / L1 |
| 冲刺 | 上者+一类证据+复测；L1 非默认 | `noise` + `baseline` | 加干预窗、等待、复测窗 | 一类证据 + 复测；最终 `verdict_4` | 默认「受控前后描述」；确认性 L1 仅当 `causal_claim=did_isolated` 且 DiD CI 不含 0 | 实际交付=06；不补开 L1 |
| 续约 | weekly+calib | `weekly` + `calib` | 监测窗 | 不新开因果，不新开处理组 | 「滚动异常」或「不能下结论」；禁止重开 L1 | 禁止用结项重开 L1 |

`sop_stage=诊断` 时，05 不得填写干预轮次卡；06 不得出现「受控前后」或 L1；07 `budget_scope` 不得含干预 / 一类证据 / 改页。

## 2. 业务逻辑（字段只写一次）

| 字段 | 写入 | 灌入 03 | 04 用法 | 05 用法 | 06 用法 | 07/08/09 |
|---|---|---|---|---|---|---|
| `{vertical}` `{city}` `{client_code}` | 01 | `project.csv` 同名列 | 进度标题 | 周报抬头 | 验收抬头 | 07 标题；08 抬头；09 结项抬头 |
| `{project_id}` `{owner}` | 02 | `project.csv` | 全套计划 | 变更/问题 | 交付注册 | 预算行；干系人；结项 |
| `treat_need_ids` | 02 需求规格 | `queries.treat=1` 且 `need_id` 在此集合 | WBS 只优化这些 need | 干预轮次卡只许勾这些 | 主表 core | 07 按这些问法估人天 |
| `holdout_need_ids` | 02 需求规格 | `queries.set=holdout` | 进度必须排监测组采集 | **禁止**改这些页 | 主表 holdout | 07 必须含监测组采集人天 |
| `platforms_required` | 02 | `platforms.csv` active | 人天按此计 | 覆盖检查 | 缺一家拒收 | 07 人天乘数 |
| `budget_hours` `budget_scope` `quote_excludes_l1` | 07 | — | 进度人天 ≤ budget_hours | 超预算走变更 | 验收不认加价 L1 | 08 不对外加卖 L1 |
| `plan_hours` | 04 | — | 必须 >0 且 ≤ `budget_hours` | 超则变更 | — | — |
| `stakeholder_decision` `comms_cadence` `comms_bound` `comms_api_not_primary` | 08 | — | — | 周报按矩阵分发 | 决策人只收四选一，不以 API 为主表 | 09 不另发明口径 |
| `intervention_need_ids` `holdout_untouched` `intervention_completed_on` `wait_days` | 05（仅冲刺） | 干预台账 | 复测窗已排 | 处理 need∈02；监测组未改 | 复测交付挂此轮 | 09 plays 只记类型 |
| `delivery_manifest_checksum` `freeze_match` `delivery_accepted` | 06 | 案件出数目录 | — | — | 未接收不得进 09 | 结项只复述已接收 |
| `{freeze_id}` | 03 第 0 天 | 冻结目录名 | 进度「第 0 天完成」 | 复测必须同一冻结 | 验收抄冻结日 | 09 结项抄冻结日 |
| `data_grade` | 03 出数 | 报告模板 | — | 周报抄 | 验收抄 | 09 抄 |
| `verdict_4` | 诊断/续约锁在 03；冲刺锁在 05 复测后 | 报告模板 | — | 冲刺本步才写最终四选一 | 验收必须相同 | 09 结项必须相同 |
| `intervention_class` | 05（仅冲刺） | `intervention_ledger.csv` | 干预窗存在才排 | 一轮一类 | 复测交付挂此轮 | 09 plays 只记类型 |
| `close_assets_ok` `close_no_reopen_l1` | 09 | 资产库登记 | — | — | — | G8 必是 |

## 3. 门（贴在全流程上）

```text
01 --G0--> 02 --G1--> 07 --G6--> 08 --G7--> 03 --G3--> 04 --G2--> 05 --G5--> 06 --G4--> 09 --G8-->
```

- G0：范围边界签字；禁售已读；若客户要保证推荐则停在 01。  
- G1：章程批准；验收标准字段齐；`sop_stage` 从意向变成锁定。  
- G6：人天按平台×问法；`quote_excludes_l1=是`；诊断 `budget_scope` 不含干预。  
- G7：干系人已登记；`comms_bound` 不得宽于四选一；决策人不以 API 为主表。  
- G3：02 的 need / 平台 / 验收已写入本案冻结且与 state 一致；采集只写 `案件/{case_id}/`。  
- G2：04 的 WBS/进度只能包含当前 `sop_stage` 允许的窗；`plan_hours`>0 且不超过 07 `budget_hours`。冲刺复测是 05 执行窗，不是 03 基线。门名只写 G2，不用 G2′。  
- G5：仅冲刺；`intervention_need_ids` ⊆ 02 处理组；完成日 + 等待；`holdout_untouched=是`。冲刺最终 `verdict_4` 只从本步复测写。  
- G4：`verdict_4` = 诊断抄 03、冲刺抄 05 复测；`freeze_match=是`；`delivery_manifest_checksum` 指向 `流程/03 测量/案件/{本案}/出数/`；`delivery_accepted=是` 才许进 09。  
- G8：资产沉淀不泄漏；结项四选一 = 06；`close_no_reopen_l1=是`；正式清单一致、中转清空、封存清单通过。

## 4. 权威

出数数字与四选一：`流程/03 测量` 手册与脚本。  
战略口径与字段所有权：本文 + `案件合同卡.md`。  
冲突时以前者为准改报告，不得以后者放宽口径。

## 5. 文件总线（10，全程并行）

`流程/10 项目文件/案件/{本案}/` 从 `init` 建到 G8 封。不插入 01–09 行走顺序，但每站材料必须进这里。

| 区 | 谁写 | 规则 |
|---|---|---|
| `原始/{阶段}/` | Agent 在人交件后 | 只追加；测量实物只登记指针 |
| `正式/现行/` + `正式/版本/` | Agent 在门 APPROVE 或 checkin 后 | 配置源 `正式/清单.csv`；禁止原地覆盖；改前 checkout |
| `中转/` | Agent drop；成员 pick/ack | 看板是实时交互面；往来留痕 |

禁售句不得入库、不得发布。09 抽查本库无客户事实。详情 `流程/10 项目文件/规则.md`。
