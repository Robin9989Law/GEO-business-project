# 案件合同卡（开单一式一份，沿 01→09 往下填）

后栏不得改前栏。变更从锁定阶段重开。行走顺序：01→02→07→08→03→04→05→06→09。

## 01 洽谈写入

| 字段 | 值 |
|---|---|
| `{vertical}` | |
| `{city}` | |
| `{client_code}` | |
| 意向 `sop_stage` | 诊断 / 冲刺 / 续约 |
| 禁售已读 | 是 / 否 |
| G0 | 开 / 过 |

## 02 需求锁定

| 字段 | 值（从此只读） |
|---|---|
| `{project_id}` | |
| `{owner}` | |
| `sop_stage`（锁定） | 必须等于或窄于 01 意向；诊断不得在此升冲刺 |
| `primary_goal` | 在无品牌发现问上提高被正确提及的概率 |
| `primary_endpoint` | p_mention |
| `causal_claim` | descriptive_until_isolation |
| `control_design` | 监测组 |
| `treat_need_ids` | |
| `holdout_need_ids` | |
| `platforms_required` | P0 |
| `success_rule` | 抄 02 验收标准对应该 `sop_stage` 的那一条 |
| G1 | 开 / 过 |

## 07 预算（测量前锁定）

| 字段 | 值 |
|---|---|
| `budget_hours` | |
| `budget_scope` | 必须等于 `sop_stage` 允许集；诊断不得含干预 / 一类证据 / 改页 |
| `quote_excludes_l1` | 是（必须是） |
| 人天按 `platforms_required` × 激活问法 | 是 / 否 |
| G6 | 开 / 过 |

## 08 沟通（测量前锁定）

| 字段 | 值 |
|---|---|
| `stakeholder_decision` | 客户决策人（只收四选一） |
| `comms_cadence` | 门通过时 / 每周 |
| `comms_bound` | 不得宽于四选一 |
| 决策人不以 API 为主表 | 是 |
| `comms_api_not_primary` | 是（必须是） |
| G7 | 开 / 过 |

## 03 测量写入

| 字段 | 值 |
|---|---|
| `{freeze_id}` | |
| 已灌入 `project.csv` 的 02 字段 | 是 / 否 |
| `treat` / `holdout` 与 02 集合一致 | 是 / 否 |
| 本阶段任务 | 诊断: noise+baseline　冲刺: +retest　续约: weekly+calib |
| `data_grade` | 定向级 / 决策级 |
| `verdict_4` | 描述基线 / 受控前后描述 / 确认性 L1 / 不能下结论 |
| `verdict_4` 落在该 `sop_stage` 允许集 | 是 / 否 |
| G3 | 开 / 过 |

## 04 计划（只排 02+03 允许的窗，不超 07）

| 字段 | 值 |
|---|---|
| 进度是否含超出 `sop_stage` 的窗 | 否（必须否） |
| 人天按 `platforms_required` × 激活问法 | 是 / 否 |
| `plan_hours` | 必须 >0 |
| 人天 ≤ `budget_hours` | 是 / 否 |
| 复测窗归属 | 05 执行（不是 03 基线） |
| G2 | 开 / 过 |

## 05 实施

| 字段 | 值 |
|---|---|
| `intervention_class` | 诊断填「无」；冲刺填一类 |
| 只动 `treat_need_ids` | 是 / 无干预 |
| 监测组未改页 | 是 |
| `holdout_untouched` | 是（冲刺必须是） |
| `intervention_need_ids` ⊆ 02 处理组 | 是 / 无干预 |
| `intervention_completed_on` / `wait_days` | 冲刺必填 |
| 周报 `verdict_4` | 诊断=03；冲刺=本步复测 |
| 对外走 08 矩阵 | 是 / 否 |
| G5 | 开 / 过 / 不适用 |

## 06 交付

| 字段 | 值 |
|---|---|
| 交付物路径 | `流程/03 测量/案件/{本案}/出数/` |
| `delivery_manifest_checksum` | |
| `freeze_match` | 是 |
| `delivery_accepted` | 是（未接收不得进 09） |
| 验收 `verdict_4` = 03 = 05 | 是 / 否 |
| `{freeze_id}` 一致 | 是 / 否（出数身份列 + manifest 哈希，不是只对冻结目录） |
| 客户可见报告（MD/PDF）已纳入哈希 | 是 / 否 |
| 覆盖齐全 | 是 / 否 |
| 禁售句 | 无 |
| 接收 | 是 / 否 |
| G4 | 开 / 过 |

## 09 收尾

| 字段 | 值 |
|---|---|
| 结项实际交付 = 06 `verdict_4` | 是 / 否 |
| `close_assets_ok` | 是 / 否 |
| `close_no_reopen_l1` | 是（必须是） |
| `close_manifest_ok` | 是 |
| `close_board_empty` | 是 |
| `close_archive_ok` | 是（deposits + 移交件 + 脱敏 + 无锁；不是只看无锁） |
| 09 三份结项草稿已在 `out/` | 是 / 否 |
| 资产库无客户事实 | 是 / 否 |
| 10 原始已登记本单材料 | 是 / 否 |
| 10 正式清单与 G0–G8 对应 | 是 / 否 |
| 10 中转无未取件 | 是 / 否 |
| G8 | 开 / 过 |

## 10 项目文件（全程）

| 项 | 值 |
|---|---|
| 本案库 | `流程/10 项目文件/案件/{本案}/` |
| 原始 | `原始/{阶段}/` + `原始/登记.csv` |
| 正式配置 | `正式/清单.csv`（doc_id / rev / checksum / gate） |
| 现行 | `正式/现行/` |
| 中转看板 | `中转/看板.md` |
