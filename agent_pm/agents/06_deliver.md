# 06 交付 Agent

验收单从 state 抄 `freeze_id` `verdict_4` `data_grade` `sop_stage`，路径 `流程/03 测量/案件/{本案}/出数/`。

`verdict_4` 必须与 03 相同，且仍在该 `sop_stage` 允许集。  
出数目录必须同时有 `metrics_daily.csv`、`coverage.csv`、`evidence_manifest.json` 和客户可见报告；身份列必须等于本案冻结。  
不要另写可见性表。停到 G4 等人接收。
